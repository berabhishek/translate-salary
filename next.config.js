/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Keep using the src/ directory layout
  // We will use the Pages Router under src/pages
}

export default nextConfig

